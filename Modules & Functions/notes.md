# Module & Functions

* Modules are defined using the `defmodule` macro.
* Functions are defined usinf the `def` macro.

    ``` elixir
        defmodule Math do
            def sum(a,b) do
                return a + b
            end
        end
    ```